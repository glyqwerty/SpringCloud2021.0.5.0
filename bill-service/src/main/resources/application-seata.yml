#seata:
#  client:
#    rm:
#      # 限制 RM 向 TC 报告状态的次数
#      report-retry-count: 5
#    tm:
#      #限制TM全局重试回滚次数
#      rollback-retry-count: 5
#      #限制 TM 发起的全局提交重试次数
#      commit-retry-count: 5
#  enabled: true
#  application-id: ${spring.application.name:default-app}   # 提供默认值
#  tx-service-group: my_test_tx_group
#
#  # 注册中心（用于发现 Seata Server）
#  registry:
#    type: nacos
#    nacos:
#      server-addr: localhost:8848
#      cluster: default
##      namespace: seata           # 与 config 保持一致
#      group: SEATA_GROUP         # 可选，默认 SEATA_GROUP
#      username: nacos
#      password: nacos
#
#  # 配置中心（用于读取 seataServer.properties）
#  config:
#    type: nacos
#    nacos:
#      server-addr: localhost:8848
##      namespace:            # 与 registry 保持一致
#      group: SEATA_GROUP
#      data-id: seataServer.properties
#      username: nacos
#      password: nacos
#
#  # 服务映射（仅配置分组到集群的映射，不再配置 grouplist）
#  service:
#    vgroup-mapping:
#      my_test_tx_group: default   # 将事务分组映射到集群 "default"